=link_to t('consultations.new'), new_consultation_path
=link_to t('users.edit'), edit_user_path(@user)
= @user.full_name
= @user.age
= @user.email

- @user.consultations.each do |consultation|
  = link_to consultation.date, consultation
